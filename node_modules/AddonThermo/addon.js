var addon = require('./build/Release/addon');

function repeat() {
    console.log(addon.begin());
do
 {
    var p = addon.ReadTemperatureUmidity(22, 4);
    var roundTemp = Math.round(p.temperatura * 100) / 100;
    var roundUmid = Math.round(p.umidita * 100) / 100;
    console.log("lettura da JAVASCRIPT -> ", roundTemp + "  " + roundUmid)
 }while(true);
}

repeat();
